import { TimecalculatePipe } from './timecalculate.pipe';

describe('TimecalculatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimecalculatePipe();
    expect(pipe).toBeTruthy();
  });
});
